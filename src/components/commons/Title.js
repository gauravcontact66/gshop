import React from 'react';

const Title = ({title}) => {
    return (
        <div>
            <h4 className="text-center text-uppercase">{title}</h4>           
        </div>
    )
}

export default Title;